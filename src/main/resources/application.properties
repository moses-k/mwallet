#logging.level.org.springframework: DEBUG
server.port=9004
#Conection to the database
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create
#mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/mwallet?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=vcuser
spring.datasource.password=vcuser123

#h2
#jdbc.driverClassName=org.h2.Driver
#jdbc.url=jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1'DATABASE_TO_UPPER=false
#hibernate.dialect=org.hibernate.dialect.H2Dialect

############################ JWT Properties ########################
jwt.expiration.time=90000
jwt.public.key=classpath:app.pub

# This is strictly for demo purpose only. Never define the private key in the repository
jwt.private.key=classpath:app.key

